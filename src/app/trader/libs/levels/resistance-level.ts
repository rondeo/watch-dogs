export class ResistanceLevel {
  constructor(private highs: number[], private lows: number[]){

  }

  ctr(){
    const highs: number[] = this.highs;
    const lows: number[] = this.lows;
    highs.forEach(function (item) {

    })
  }
}
